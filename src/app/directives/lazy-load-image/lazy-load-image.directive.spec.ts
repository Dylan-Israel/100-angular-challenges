import { LazyLoadImageDirective } from './lazy-load-image.directive';

describe('LazyLoadImageDirective', () => {
  it('Exists', () => {
    // assert
    expect(LazyLoadImageDirective).toBeDefined();
  });
});
